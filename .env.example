# MongoDB Atlas – connection URI (from Atlas: Connect → Drivers → Node.js)
# Use "timesheet" as db name – DB and "users" collection are created on first write
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/timesheet?retryWrites=true&w=majority

# Cloudinary – from Dashboard: Account → API Keys
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Optional: folder prefix for uploads (e.g. "timesheet")
CLOUDINARY_UPLOAD_FOLDER=timesheet

# Optional: secret for cron routes (e.g. cleanup-cloudinary). Set in production; send as Bearer token or ?secret=
# CRON_SECRET=your_random_secret

# Auth – required for login. JWT signing secret (min 32 chars).
# Generate one: openssl rand -hex 32
JWT_SECRET=replace_with_at_least_32_character_secret_string_here